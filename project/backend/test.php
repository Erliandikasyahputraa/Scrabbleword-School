<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $app->make(\Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $sub = \App\Models\Submission::first(); echo json_encode($sub ? $sub->toArray() : null, JSON_PRETTY_PRINT);
